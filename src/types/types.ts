export interface ButtonProps {
    text: string;
    path: string;
}

export interface ShowItemProps {
    key: number;
    date: string;
    venue: string;
    location: string;
    country: string;
    tickets: string;
}